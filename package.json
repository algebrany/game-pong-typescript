{
  "scripts": {
    "prebuild": "tstl",
    "build": "gly-cli build-html --cwd build src/game.lua --fengari --enginecdn --atob",
    "start": "npx serve -l 80 ./dist"

  },
  "dependencies": {
    "@gamely/gly-cli": "0.1.2",
    "@gamely/gly-types": "0.1.2",
    "typescript-to-lua": "^1.31.2"
  }
}
